<h1>This is the chef index page</h1>
<hr>
<div class="menu">
  <%= link_to "Add A Chef", new_chef_path, class: "menu_item"%>
</div>
<hr>

<div class="row">
  <% @chefs.each do |chef| %>
  <div class="col-xs-12 col-sm-4">
    <div class="card" >
      <div class="card-description">
      </div>
    </div>
    <!-- <div class="row"> -->
      <div class="chef-details row">
        <div class="col-xs-8">
          <div class="chef-name">
            <h4> <%= link_to chef.name, chef_path(chef) %> </h4>
            <p> <%= chef.title %></p>
          </div>
        </div>
        <div class="col-xs-4">
          <div class="price">
            <span><%= chef.price %>€</span> <span>pp</span>
          </div>
        </div>
      </div>
      <!-- </div> -->
    </div>
    <% end %>
  </div>


