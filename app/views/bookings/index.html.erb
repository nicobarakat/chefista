<h1>Bookings:</h1>

<% @bookings.each do |booking| %>
<h2> <%= link_to booking.date, booking_path(id: booking.id) %> </h2>
<p> <%= link_to 'Delete', booking_path(booking), method: :delete %> </p>
<% end %>
